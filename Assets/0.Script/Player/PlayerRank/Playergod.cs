using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Playergod : Player
{
    // Start is called before the first frame update
    void Start()
    {
        Init(8); 
    }

}
